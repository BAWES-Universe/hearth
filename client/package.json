{
  "name": "hearth-client",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Hearth PWA client — PixiJS v8 world + Preact chat, per PROTOCOL.md",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "icons": "python3 scripts/gen-icons.py",
    "mock": "node scripts/mock-server.mjs"
  },
  "dependencies": {
    "pixi.js": "^8.6.6",
    "preact": "^10.25.4"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.1",
    "typescript": "^5.7.3",
    "vite": "^6.0.7",
    "vite-plugin-pwa": "^0.21.1"
  }
}
